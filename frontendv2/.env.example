# Optional. Local dev default is http://localhost:5000
# Production on Vercel uses /api proxy in vercel.json → Render (no .env needed)
# VITE_BACKEND_URL=http://localhost:5000
# VITE_BACKEND_URL=https://qbeta-v2.onrender.com
